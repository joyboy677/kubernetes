apiVersion: v1
kind: Service
metadata:
  labels:
    app: todo-app-svc
  name: todo-app-svc
spec:
  ports:
    - port: 1234
      targetPort: 3000
      nodePort: 30080
  selector:
    app: todo-app-deployment
  type: NodePort
