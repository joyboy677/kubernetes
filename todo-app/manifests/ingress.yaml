apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  name: my-ingress
  namespace: project
spec:
  rules:
    - host: localhost
      http:
        paths:
          - backend:
              service:
                name: todo-app-svc
                port:
                  number: 1234
            path: /
            pathType: Prefix
status:
  loadBalancer: {}
